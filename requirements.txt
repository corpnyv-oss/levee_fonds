django>=5.2.5
psycopg2-binary
dj-database-url>=2.1.0
djangorestframework
djangorestframework-simplejwt
django-cors-headers
django-axes
# Two-factor/OTP removed for now to simplify dependencies; add back if needed.
# django-two-factor-auth
# django-otp
# django-otp-totp
# django-otp-static
django-celery-results
django-celery-beat
celery
redis
# django-storages
# boto3
drf-yasg
django-environ
python-dotenv
django-extensions
gunicorn
whitenoise
requests
django-csp
django-db-geventpool
djangorestframework-camel-case
# Audit (keep)
django-simple-history
# ClamAV integration optional; leave commented unless daemon present
# pyclamd

# Security dependencies
python-json-logger>=2.0.7
django-axes>=8.0.0  # Pour la gestion des tentatives de connexion
argon2-cffi>=23.1.0  # Pour le hachage sécurisé des mots de passe
cryptography>=42.0.0  # Pour le chiffrement
python-jose[cryptography]>=3.3.0  # Pour les JWT
sentry-sdk>=1.39.1  # Pour la surveillance des erreurs
# django-csp est déjà dans les dépendances principales